---

---

<section id="quotes" class="relative z-0 py-12 flex flex-col items-center justify-center gap-8 px-4  md:-mb-40">
  <div class="relative z-10 flex flex-col items-center justify-center gap-5">
    <h2
      class="text-[37px] md:text-[58px] w-full max-w-[14ch] md:max-w-max text-center  font-normal leading-none text-[#383838]  tracking-tight"
      >
      Orçamentos Instantâneos.<br class="hidden md:block"/> Mais clientes atendidos, muito mais rápido.
    </h2>
    
    <p class="text-sm md:text-[17px] text-center  leading-snug tracking-[-0.02em] max-w-[36ch] md:max-w-[47ch] " >
      A IA da Sharpi envia orçamentos 24/7, liberando seu time para focar em relacionamentos que impulsionam o crescimento.
    </p>
  </div>

  <div class="z-0 w-full max-w-7xl  rounded-[20px] overflow-hidden ">
    <video
      id="quotes-video"
      src="/assets/medias/index/quotes/quotes.mp4"
      autoplay
      loop
      muted
      class="w-full h-full object-cover"
    >
    </video>
  </div>
</section>

<script>
  import { inView } from "motion";

  const initQuotesVideo = () => {
    const video = document.querySelector<HTMLVideoElement>("#quotes-video");

    if (video) {
      inView(
        video,
        () => {
          video.currentTime = 0;
          video.play().catch(() => {

          });

          return () => {
            video.pause();
          };
        },
        { amount: 0.2 }
      );
    }
  };

  if (document.readyState === "loading") {
    document.addEventListener("DOMContentLoaded", initQuotesVideo);
  } else {
    initQuotesVideo();
  }
</script>