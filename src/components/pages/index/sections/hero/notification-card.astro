---
interface Props {
  title: string;
  message: string;
  media?: string;
  index: number;
  type: "image" | "video";
}

const { title, message, media, index, type } = Astro.props;
---

<div
  id={`notification-card-${index}`}
  class="relative z-0 w-[256px]  md:w-[240px] rounded-[20px] md:rounded-[18px] origin-top shadow-lg backdrop-blur-lg py-4 md:py-[15px] px-[12.5px] md:px-3 mb-2 pointer-events-none"
  data-index={index}
  style="background-color: #FFFFFF40;"
>
  <div class="relative z-10 flex items-start gap-3 md:gap-2">
    <div class="flex-shrink-0  bg-zinc-200 rounded overflow-hidden">
      {media && type === "video" && <video src={media} autoplay loop muted playsinline class="size-[20px] md:size-[20px]  object-cover"/>}
      {media && type === "image" && <img src={media} alt={title} class="w-[25px] h-[30px] md:w-[23px] md:h-[28px]  object-cover"/>}
    </div>

    <div class="flex-1 min-w-0 space-y-1">
      <h4 class="text-[14.5px] md:text-[13.5px] leading-none font-light md:font-normal text-[#90908E] truncate whitespace-nowrap">
        {title}
      </h4>
      <p class="text-[14.5px] md:text-[13.5px] leading-none font-medium md:font-normal text-[#2D2D2D] whitespace-nowrap">
        {message}
      </p>
    </div>
  </div>
</div>
